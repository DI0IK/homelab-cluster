---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qui-config-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client
  annotations:
    nfs.io/storage-path: qui
