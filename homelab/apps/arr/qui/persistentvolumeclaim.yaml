---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qui-config-pvc
  annotations:
    nfs.io/storage-path: qui
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client
