---
apiVersion: v1
kind: Service
metadata:
  name: qui
  namespace: arr
  labels:
    app: qui
spec:
  selector:
    app: qui
  ports:
    - protocol: TCP
      port: 7476
      name: webui
      targetPort: webui
